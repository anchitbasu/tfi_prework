<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="intern.tfi.org.prework">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="TFI"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Material.NoActionBar.Fullscreen">

        </activity>
        <activity
            android:name=".VolunteerStart"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Material" />
        <activity
            android:name=".UserSignUpActivity"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Material" />
        <activity
            android:name=".FellowStartActivity"
            android:label="@string/title_activity_fellow_start"
            android:screenOrientation="portrait" />
        <activity
            android:name=".AdminActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".StaffLoginActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".SearchCriteriaActivity" />
        <activity android:name=".SplashActivity" android:theme="@android:style/Theme.Material.NoActionBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>